# AI/BI Dashboards (ex-Slate)

Export Slate KPI definitions into SQL in `src/semantic/metrics/`, then build dashboards in the workspace UI.

Optional: store `.lvdash.json` exports here for version control once created.
